<div class="text-center">
  <h1>Add new material</h1>
</div>
<div>
  <%= form_for(@material) do |form| %>
    <%= render 'create_material_fields', form: form %>
    <%= form.submit "Add material", class: "btn btn-large btn-primary" %>
  <% end %>
</div>
