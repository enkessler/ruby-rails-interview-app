<div class="text-center">
  <h1>Create new Users</h1>
</div>
<div>
  <%= form_for(@user) do |form| %>
      <%= render 'create_fields', form: form %>
      <%= form.submit "Create a new User", class: "btn btn-large btn-primary" %>
  <% end %>
</div>
