<div class="text-center">
  <h1>Edit <%= @user.name %></h1>
</div>

<div>
  <%= form_for(@user) do |form| %>
      <%= render 'create_fields', form: form %>
      <%= form.submit "Update employee", class: "btn btn-large btn-primary" %>
  <% end %>
</div>
